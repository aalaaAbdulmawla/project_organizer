<%= form_for(@user_story_comment) do |f| %>
  <% if @user_story_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_story_comment.errors.count, "error") %> prohibited this user_story_comment from being saved:</h2>

      <ul>
      <% @user_story_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="field">
    <%= f.label :creator %><br>
    <%= f.number_field :creator %>
  </div>
  <div class="field">
    <%= f.label :user_story_id %><br>
    <%= f.text_field :user_story_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
